/**
 * scoring.ts — 담당: 주영
 *
 * TODO:
 * - 사용자 피드백(좋아요/싫어요/스킵)을 바탕으로 키워드/카테고리 점수 갱신
 */

export function updateKeywordScore(
  currentScore: number,
  feedback: "like" | "dislike" | "skip"
): number {
  // TODO: 주영 - 가중치 업데이트 로직
  return currentScore;
}
